#include <stdio.h>
int factorial(int n)
{
	if (n==0)
	return 1;
	else
	return(n*factorial(n-1));
}
int main()
{
	int n;
	int fact;
	printf("enter the number:");
	scanf("%d",&n);
	fact=factorial(n);
	printf("the factorail of%d is:%d",fact);
	return 0;
}
