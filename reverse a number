#include<stdio.h>

int main()
{
    int n,rem,res=0;
    printf("enter  the number: ");
    scanf("%d",&n);
    while(n!=0)
    {
        rem = n%10;
        res = res*10+rem;
        n/=10;
    }
    printf("%d",res);
    return 0;
}
